<%= form_with(model: [:admin, instagram_content], local: true) do |form| %>
  <% if instagram_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instagram_content.errors.count, "error") %> prohibited this instagram_content from being saved:</h2>

      <ul>
      <% instagram_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
     <%= form.label :images %>
     <%= form.file_field :images, multiple: true %>
     <%= form.hidden_field :avatar_cache %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
