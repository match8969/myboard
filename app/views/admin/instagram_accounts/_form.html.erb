<%= form_with(model: [:admin, instagram_account], local: true) do |form| %>
  <% if instagram_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instagram_account.errors.count, "error") %> prohibited this instagram_account from being saved:</h2>

      <ul>
      <% instagram_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
