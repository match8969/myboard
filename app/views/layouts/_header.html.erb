<nav class="navbar navbar-expand-md navbar-light bg-dark mt-3 mb-3">
  <div class="container">
    <ul class="navbar-nav ml-auto">
      <a class="navbar-brand " href="/">
        <%= link_to 'My board', root_path, class: "nav-link" %>
      </a>
    </ul>

    <% if user_signed_in? %>
      <%= image_tag current_user.image.thumb.url if current_user.image? %>
      Login User:<%= current_user.email %>
      <div class="collapse navbar-collapse justify-content-end">
        <ul class="navbar-nav">
          <li><%= link_to 'Add Instatgram ', new_admin_instagram_account_path, class: "nav-link" %></li>
          <li><%= link_to 'Your Instagram Feed List', admin_instagram_accounts_path, class: "nav-link" %></li>
          <li class="nav-item active dropdown">
            <div class="nav-link dropdown-toggle" role="button" data-toggle="dropdown"></div>
            <div class="dropdown-menu">
              <%= link_to 'Log Out', destroy_user_session_path, class: "dropdown-item" %>
              <%= link_to 'Edit Your Profile', edit_user_registration_path, class: "dropdown-item" %>
            </div>
          </li>
        </ul>
      </div>
    <% else %>
      <%= link_to 'Log in', user_session_path %>
      <%= link_to 'Sign in', new_user_registration_path %>
    <% end %>
  </div>
</nav>

